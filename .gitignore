# cross-agent review handoff — local scratch (the protocol's own folder)
/.cross-review/

# Python
__pycache__/
*.pyc

# MP4 render is generated on demand for social posts, not shipped (GIF is the repo asset)
/assets/cross-review-flow.mp4
